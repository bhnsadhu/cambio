# Supabase project (public; safe to ship to the browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Shared secret that lets the Next.js server call the SECURITY DEFINER game_* RPCs.
# Must match private.server_config.server_secret in the database.
CAMBIO_SERVER_SECRET=
